#include "MusicInfo.hpp"

MusicInfo::MusicInfo(String folderPath, String title, String artist, String bpm, double offset, Array<int> playLevels, String assetName):
  folderPath(folderPath), title(title), artist(artist), bpm(bpm), offset(offset), playLevels(playLevels), assetName(assetName){

}